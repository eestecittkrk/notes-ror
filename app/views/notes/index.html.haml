- @notes.each do |note|
  %p
    Name:
    = note.name
  %p
    Content:
    = note.content
  %p
    = link_to "Edit note", edit_note_path(note)
    = link_to "Show note", note_path(note)
    = link_to "Delete note", note_path(note), method: :delete  
%p= link_to "New note", new_note_path